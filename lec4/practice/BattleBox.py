# -*- coding:utf-8 -*-

from typing import *
from Settings import BattleSettings, GamePath
import pygame

class BattleBox:
    def __init__(self, window, player, monster, fontSize: int = BattleSettings.textSize, 
                 fontColor: Tuple[int, int, int] = (255, 255, 255), bgColor: Tuple[int, int, int, int] = (0, 0, 0, BattleSettings.boxAlpha)) :
        pass

        # 初始化窗口和字体
    
        # 初始化相关角色的参数，没有实际操作的权力

        # 默认玩家先手

        # 区分放动画状态和攻击结算状态

        # 是否结束


    def render(self):
        pass

        # 绘制背景和文字
        
        # 绘制战斗过程

        # 战斗判定以及结算

        # 战斗结束
        
